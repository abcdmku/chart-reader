{
  "name": "chart-reader",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm --workspace apps/server run dev\" \"npm --workspace apps/web run dev\"",
    "build": "npm --workspace apps/web run build && node scripts/sync-web-dist.mjs && npm --workspace apps/server run build",
    "start": "npm --workspace apps/server run start",
    "db:studio": "npm --workspace apps/server run db:studio",
    "test": "npm --workspace apps/server run test"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "drizzle-orm": "^0.45.1"
  }
}
